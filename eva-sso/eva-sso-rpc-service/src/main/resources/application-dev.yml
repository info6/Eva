zookeeper:
  address: zookeeper://127.0.0.1:2181


spring:
  jpa:
      show-sql: true
      database: mysql
      generate-ddl: false

  datasource:
      url: jdbc:mysql://localhost/sso?useSSL=false
      username: root
      password:
      type: com.alibaba.druid.pool.DruidDataSource
      driver-class-name: com.mysql.jdbc.Driver